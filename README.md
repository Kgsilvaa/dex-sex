<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dry Sex AI | Premium Generation</title>
    <style>
        :root {
            --preto-profundo: #0a0a0a;
            --preto-card: #141414;
            --dourado-brilhante: #e5c05b;
            --dourado-dark: #b89130;
            --texto-cinza: #a0a0a0;
        }

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--preto-profundo);
            color: white;
            height: 100%;
            display: flex;
        }

        /* Barra Lateral */
        nav {
            width: 260px;
            background-color: #000;
            border-right: 1px solid #222;
            display: flex;
            flex-direction: column;
            padding: 20px;
            justify-content: space-between;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--dourado-brilhante);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 30px;
            text-align: center;
            border-bottom: 1px solid var(--dourado-dark);
            padding-bottom: 10px;
        }

        .menu-item {
            padding: 12px;
            color: var(--texto-cinza);
            text-decoration: none;
            border-radius: 8px;
            margin-bottom: 5px;
            transition: 0.3s;
        }

        .menu-item:hover {
            background-color: #1a1a1a;
            color: var(--dourado-brilhante);
        }

        /* Área Principal */
        main {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        header {
            padding: 15px 40px;
            border-bottom: 1px solid #222;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .status-badge {
            background: linear-gradient(45deg, var(--dourado-dark), var(--dourado-brilhante));
            color: black;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
        }

        /* Chat e Output */
        .content-area {
            flex-grow: 1;
            overflow-y: auto;
            padding: 40px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .welcome-text {
            text-align: center;
            margin-top: 50px;
        }

        .welcome-text h1 {
            font-size: 2.5rem;
            background: linear-gradient(to right, #fff, var(--dourado-brilhante));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Barra de Prompt */
        .input-container {
            width: 100%;
            max-width: 800px;
            padding: 20px 40px;
            background-color: var(--preto-profundo);
            position: sticky;
            bottom: 0;
        }

        .prompt-wrapper {
            background-color: var(--preto-card);
            border: 1px solid #333;
            border-radius: 15px;
            padding: 15px;
            display: flex;
            align-items: center;
            gap: 15px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.5);
        }

        .prompt-wrapper:focus-within {
            border-color: var(--dourado-dark);
        }

        textarea {
            flex-grow: 1;
            background: transparent;
            border: none;
            color: white;
            resize: none;
            outline: none;
            font-size: 1rem;
            height: 24px;
        }

        .btn-generate {
            background: linear-gradient(45deg, var(--dourado-dark), var(--dourado-brilhante));
            border: none;
            border-radius: 8px;
            padding: 10px 20px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.2s;
        }

        .btn-generate:hover {
            transform: scale(1.05);
            box-shadow: 0 0 15px rgba(229, 192, 91, 0.4);
        }

        .options-row {
            display: flex;
            gap: 20px;
            margin-top: 10px;
            font-size: 0.85rem;
            color: var(--texto-cinza);
        }

        .option {
            cursor: pointer;
        }

        .option:hover {
            color: var(--dourado-brilhante);
        }
    </style>
</head>
<body>

    <!-- Barra Lateral -->
    <nav>
        <div>
            <div class="logo">DRY SEX AI</div>
            <div style="display: flex; flex-direction: column;">
                <a href="#" class="menu-item">✦ Nova Geração</a>
                <a href="#" class="menu-item">📸 Minha Galeria</a>
                <a href="#" class="menu-item">🎬 Vídeos Gerados</a>
                <a href="#" class="menu-item">🔥 Modelos Hot</a>
            </div>
        </div>
        
        <div style="border-top: 1px solid #222; pt-20px;">
            <a href="#" class="menu-item" style="color: var(--dourado-brilhante);">⭐ Upgrade para Pro</a>
            <a href="#" class="menu-item">⚙️ Configurações</a>
        </div>
    </nav>

    <!-- Área Principal -->
    <main>
        <header>
            <div class="status-badge">V1.0 STABLE ADULT</div>
            <div style="font-size: 0.9rem;">Créditos: <span style="color: var(--dourado-brilhante);">500</span></div>
        </header>

        <div class="content-area">
            <div class="welcome-text">
                <h1>O que vamos criar agora?</h1>
                <p style="color: var(--texto-cinza);">Gere imagens realistas ou vídeos de até 60s sem censura.</p>
            </div>
            
            <!-- Aqui apareceriam os resultados gerados -->
        </div>

        <div class="input-container">
            <div class="prompt-wrapper">
                <textarea placeholder="Descreva a cena em detalhes..."></textarea>
                <button class="btn-generate">GERAR</button>
            </div>
            <div class="options-row">
                <div class="option">🖼️ Imagem (HQ)</div>
                <div class="option">🎥 Vídeo (60s)</div>
                <div class="option">📐 Aspecto 9:16</div>
                <div class="option">✨ Ultra Realismo</div>
            </div>
        </div>
    </main>

</body>
</html>
